# FileBeam Work Log

## Feature: downloads-info-and-folder-rename

- [x] Plan Approved
- [x] Branch Created | [x] Implementation | [x] Testing | [x] Documentation | [x] Merged

**Last Success:** Merged feature/downloads-info-and-folder-rename into main (182 tests passing)
**Next Step:** Feature complete — no pending work
**Context Note:** 182 tests passing on main. 3 new endpoints + UI updates in HtmlRenderer.

---

## Feature: config-file-and-invite-system (multi-phase)

Design approved in session. Implementing in sequential branches, one phase at a time.

### Phase 1: Config file support
- [x] Plan Approved
- [x] Branch Created | [x] Implementation | [x] Testing | [x] Documentation | [x] Merged

**Last Success:** Merged feature/config-file into main (182 tests passing). README updated.
**Deliverables:** `src/FileBeamConfig.cs`, `--config` flag, CWD auto-discovery of `filebeam.json`, `--print-config` flag.

### Phase 2: Invite system backend
- [x] Plan Approved
- [x] Branch Created | [x] Implementation | [x] Testing | [x] Documentation | [x] Merged

**Last Success:** Merged feature/invite-system into main (206 tests passing). README updated.
**Deliverables:** `src/InviteStore.cs`, `--invites-file` flag, sessionKey, CSRF header support, admin invite endpoints, `/join/{token}`, 24 new tests.

**Scope:**
- `InviteToken` record: Id (12-char URL-safe token), FriendlyName, Role, ExpiresAt (nullable), UseCount, CreatedBy, IsActive, CreatedAt
- `InviteStore` class: in-memory ConcurrentDictionary; optional persistence via `--invites-file <path>` (JSON, auto-save on create/revoke/edit, auto-load on startup)
- `--invites-file` flag added to Program.cs (parsed + forwarded to RouteHandlers)
- Endpoints: `POST /admin/invites` (JSON + X-CSRF-Token), `GET /admin/invites` (list JSON), `DELETE /admin/invites/{id}` (X-CSRF-Token), `PATCH /admin/invites/{id}` (JSON + X-CSRF-Token)
- CSRF middleware updated to also accept `X-CSRF-Token` header (DELETE/PATCH and JSON POST)
- sessionKey: 32 random bytes at startup; passed to RouteHandlers for cookie signing
- `GET /join/{token}` — validates token, sets signed `fb.session` cookie (HMAC-SHA256), increments UseCount, redirects to `/`

### Phase 3: Cookie session auth
- [x] Plan Approved
- [x] Branch Created | [x] Implementation | [x] Testing | [x] Documentation | [x] Merged

**Last Success:** Merged feature/cookie-session into main (220 tests passing). README updated.
**Deliverables:** `RouteHandlers.TryValidateSessionCookie` (public static), cookie auth in middleware, welcome page for `/join/{token}`, 14 new tests in `CookieSessionTests.cs`.

### Phase 4: Admin UI — Invites page
- [x] Plan Approved
- [x] Branch Created | [x] Implementation | [x] Testing | [x] Documentation | [x] Merged

**Last Success:** Merged feature/admin-invites-ui into main (235 tests passing). README updated.
**Deliverables:** `HtmlRenderer.RenderInvitesAdmin`, `BuildNavLinks` `hasInvites` param, content-negotiated `GET /admin/invites`, 15 new tests.

### Phase 5: Admin UI — Config export modal
- [x] Plan Approved
- [x] Branch Created | [x] Implementation | [x] Testing | [x] Documentation | [x] Merged

**Last Success:** Merged feature/admin-config-export into main (258 tests passing). README updated.
**Deliverables:** `FileBeamConfig.ToCliCommand`, `HtmlRenderer.BuildAdminConfigModal`, `hasConfig` param on `BuildNavLinks`, `adminConfigModal` param on `RenderDirectory`, `RouteHandlers.GetAdminConfig`, `GET /admin/config`, `{{ADMIN_CONFIG_MODAL}}` placeholder in index.html, 23 new tests, `src/Properties/AssemblyInfo.cs` (InternalsVisibleTo).

**Next Step:** Feature complete — no pending work
**Context Note:** 258 tests passing on main.

---

## Bug fixes: clipboard, per-sender invite key, remove preview button

- [x] Plan Approved
- [x] Branch Created | [x] Implementation | [x] Testing | [x] Documentation | [x] Merged

**Last Success:** Merged feature/bug-fixes-clipboard-sender-preview into main (258 tests passing).
**Next Step:** Feature complete — no pending work
**Context Note:** 258 tests passing on main.

---

## Feature: admin-bearer-auth

Replace `--credentials-file` / `--password` with a built-in admin account and Bearer token auth via invite tokens.

- [x] Plan Approved
- [x] Branch Created | [x] Implementation | [x] Testing | [x] Documentation | [x] Merged

**Last Success:** Merged feature/admin-bearer-auth into main (246 tests passing).
**Next Step:** Feature complete — no pending work
**Context Note:** 246 tests passing on main. CredentialStore.cs deleted; AdminAuth.cs added; 20 new AdminBearerAuthTests.cs tests. Fixed TryValidateSessionCookie to use invite.FriendlyName.

### Approved Plan

**Branch:** `feature/admin-bearer-auth`

#### Removed
- `--credentials-file` and `--password` CLI flags
- `src/CredentialStore.cs` (entire file)
- Credential store tests

#### New CLI flags
- `--admin-username` (default: `"admin"`)
- `--admin-password` — if omitted: generate random 16-char password, write to `filebeam-admin.key` in CWD, print to terminal; on restart without flag read key file so password survives; `FILEBEAM_ADMIN_PASSWORD` env var takes precedence over both

#### Auth middleware order
1. Resolve admin password: env var → CLI flag → `filebeam-admin.key`
2. Basic Auth: match admin username + password → role `"admin"`
3. Bearer token: `Authorization: Bearer {id}` → `inviteStore.TryGet(id)` (NOT TryRedeem — no UseCount increment) → apply invite role/user
4. Session cookie: unchanged

#### HtmlRenderer — create modal
After creation show two copy fields:
- Browser: `https://host/join/{id}` — existing
- CLI: `Bearer {id}` — new (copies full header value)

#### HtmlRenderer — invite table rows
- 🔗 button: copies browser join URL (unchanged)
- ⌨ button (new): copies `Bearer {id}` (full header value)
- Inline note below table: *"The Bearer token and join link share the same ID — sharing the link grants both browser and API access."*

#### Files to change
| File | Change |
|---|---|
| `src/Program.cs` | Remove old flags; add admin flags, env var + key file logic; print/persist auto-gen password; simplify auth middleware |
| `src/RouteHandlers.cs` | Remove `credentialStore` param; Bearer check uses `TryGet` |
| `src/FileBeamConfig.cs` | Remove credentials-file fields; `adminUsername` can be config field; `adminPassword` intentionally excluded |
| `src/CredentialStore.cs` | **Delete** |
| `src/HtmlRenderer.cs` | Create modal: two copy fields; invite rows: ⌨ copy button + shared-ID note |
| `tests/` | Remove `CredentialStoreTests.cs`; add tests for admin Basic Auth, Bearer (active/expired/revoked/UseCount unchanged), env var precedence, key file read |
| `README.md` | Auth section: admin setup, key file, env var, Bearer CLI usage, TLS note, shared-ID warning |

---

## Feature: invite-expiry-countdown

- [x] Plan Approved
- [x] Branch Created | [x] Implementation | [x] Testing | [x] Documentation | [x] Merged

**Last Success:** Merged feature/invite-expiry-countdown into main (248 tests passing).
**Next Step:** Feature complete — no pending work
**Context Note:** 248 tests passing on main. data-expires ISO 8601 attribute + title tooltip on expiry cells. Inline JS _fmtExpiry() + initExpiryCountdowns() added to RenderInvitesAdmin script block. Expired invites → red "expired Xm ago". 2 new tests.

---

## Feature: invite-max-uses (FB-01)

- [x] Plan Approved
- [x] Branch Created | [x] Implementation | [x] Testing | [x] Documentation | [x] Merged

**Last Success:** Merged feature/invite-max-uses into main (268 tests passing).
**Next Step:** Feature complete — no pending work
**Context Note:** 268 tests passing. Added JoinMaxUses/BearerMaxUses/BearerUseCount to InviteToken; TryBearerAuthenticate in InviteStore; AdminAuth.TryBearerAuth delegates to TryBearerAuthenticate; modal dropdowns for both caps; Uses column shows X/N. 20 new tests.

---

#### Security decisions
- `TryGet` (not `TryRedeem`) for Bearer — UseCount only reflects browser join events
- `FILEBEAM_ADMIN_PASSWORD` env var avoids password in process list
- `filebeam-admin.key` persists auto-gen password across restarts
- `adminPassword` excluded from config export (intentional)
- README notes: TLS recommended for Bearer; shared-ID warning for invite links
